{
  "name": "turtle-clicker",
  "version": "1.0.0",
  "description": "An idle game based on turtles",
  "author": "Powerlated",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "test": "jest",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "dev": "nuxt --hostname 0.0.0.0 --port 3001",
    "fix": "nuxt --fix",
    "generate": "nuxt generate",
    "start": "nuxt start --hostname 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "cross-env": "^5.2.0",
    "fastify": "^1.13.3",
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/pwa": "^3.0.0-0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "eslint-config-prettier": "^4.1.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "^1.16.4"
  }
}
